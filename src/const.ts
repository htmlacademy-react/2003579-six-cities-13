export enum AppRoute {
  Favorites = '/favorites',
  Login = '/login',
  NotFoudPage = '*',
  Offer = '/offer:id',
  Root = '/'
}

export enum AuthorizationStatus {
  Auth = 'AUTH',
  NoAuth = 'NO_AUTH',
  Unknown = 'UNKNOWN',
}
